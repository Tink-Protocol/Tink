// kept as stubs for local testing. Frontend will use Phantom directly.
export const payWithPhantom = async (amount: number) => {
  throw new Error(
    "payWithPhantom should be done by the frontend via Phantom wallet."
  );
};
